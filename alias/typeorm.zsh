# TYPEORM OPERATIONS

alias nmg='npm run migration:generate'
alias nmc='npm run migration:create'
alias nmr='npm run migration:run'
alias nmrt='npm run migration:revert'


# TYPEORM YARN OPERATIONS

alias ymg='yarn migration:generate'
alias ymc='yarn migration:create'
alias ymr='yarn migration:run'
alias ymrt='yarn migration:revert'
